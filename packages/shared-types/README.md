# shared-types

Pacchetto riservato per tipi/DTO condivisi tra `apps/web` e `apps/api` (es. shape delle
risposte API, enum di stato ordine). Volutamente vuoto in M0: verrà popolato solo quando
la duplicazione di tipi tra frontend e backend diventerà un problema reale (YAGNI).
